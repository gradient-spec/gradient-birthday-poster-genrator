# ============================================================
# Birthday Poster Generator — production dependencies
# Audited 2026-07-28. Only packages directly imported or
# required as hard transitive deps of the packages below.
# ============================================================

# ── Web framework ────────────────────────────────────────────
Flask==3.1.3
gunicorn==26.0.0

# Flask hard deps (pulled in automatically, pinned for
# reproducibility on Render):
blinker==1.9.0
click==8.3.1
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
Werkzeug==3.1.8

# ── Supabase (DB + Storage + Auth) ───────────────────────────
supabase==2.31.0

# supabase hard deps:
httpx==0.28.1
postgrest==2.31.0
realtime==2.31.0
storage3==2.31.0
supabase-auth==2.31.0
supabase-functions==2.31.0

# httpx hard deps:
anyio==4.12.1
certifi==2026.1.4
httpcore==1.0.9
idna==3.11

# realtime / storage3 / supabase-auth hard deps:
deprecation==2.1.0
pydantic==2.12.5
pydantic_core==2.41.5
PyJWT==2.13.0
typing_extensions==4.15.0
typing-inspection==0.4.2
websockets==15.0.1
yarl==1.24.5
multidict==6.7.1
propcache==0.5.2
StrEnum==0.4.15

# gunicorn hard dep:
packaging==26.2

# ── Image generation (poster composition) ────────────────────
pillow==12.2.0

# ── HTTP client (photo + template download, Telegram API) ────
requests==2.32.5

# requests hard deps:
charset-normalizer==3.4.4
urllib3==2.6.3

# ── Environment / config ─────────────────────────────────────
python-dotenv==1.2.1

# ── Timezone data (zoneinfo / IST in core/db.py) ─────────────
tzdata==2026.1

# ── File uploads (Flask multipart form parsing) ──────────────
python-multipart==0.0.22

# ── annotated-types (pydantic_core hard dep) ─────────────────
annotated-types==0.7.0
